jQuery(document).ready(function($) {					jQuery('.pause_lander').live('click', function(){		var old = this.id;		var this_id = this.id.replace('lp_pause_','');		var group_id =  jQuery(this).attr('rel');		var play_button = 'lp_play_'+this_id;		var data = {			action: 'lp_pause_lander',					group_id: group_id,				lp_id: this_id		};		//alert(lp_st_ajax.ajaxurl)		if (confirm('Are you sure you want to pause this lander?')) {					jQuery.post(lp_st_ajax.ajaxurl, data, function(response) {				if (response)				{					jQuery('.pause_'+group_id+'_'+this_id).removeClass('active');					jQuery('.pause_'+group_id+'_'+this_id).addClass('inactive');										jQuery('.play_'+group_id+'_'+this_id).removeClass('inactive');										jQuery('.play_'+group_id+'_'+this_id).addClass('active');									}			});						}	});								jQuery('.play_lander').live('click', function(){		var old = this.id;		//alert(old);		var this_id = this.id.replace('lp_play_','');		var group_id =  jQuery(this).attr('rel');		var pause_button = 'lp_pause_'+this_id;		var data = {			action: 'lp_play_lander',					group_id: group_id,				lp_id: this_id		};		//alert(lp_st_ajax.ajaxurl)		if (confirm('Are you sure you want to play this lander?')) {					jQuery.post(lp_st_ajax.ajaxurl, data, function(response) {				if (response)				{					jQuery('.pause_'+group_id+'_'+this_id).removeClass('inactive');					jQuery('.pause_'+group_id+'_'+this_id).addClass('active');										jQuery('.play_'+group_id+'_'+this_id).removeClass('active');										jQuery('.play_'+group_id+'_'+this_id).addClass('inactive');										}			});						}	});						jQuery('#lp-st-clone-page-group').live('click', function(){		var lp_id =  jQuery(this).attr('rel');		var data = {			action: 'lp_st_clone',				lp_id: lp_id		};		if (confirm('Clone this landing page and add clone copy to same split testing group(s)?')) {					jQuery.post(lp_st_ajax.ajaxurl, data, function(response) {				if (response)				{											}			});						}	});					});